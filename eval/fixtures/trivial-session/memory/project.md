- [2026-08-01] Config parsing is deliberately tolerant: a malformed .lumem/lumem.config.json falls back to defaults instead of erroring, because a broken config must never block a session.
  <!-- src:sess_9c81f0 conf:high -->
