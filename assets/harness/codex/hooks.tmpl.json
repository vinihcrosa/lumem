{
  "_lumem_note": "Stub template. Exact Codex hooks.json schema is finalized and integration-tested in T33.",
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": ["node", "{{HOOK_BUNDLE}}", "codex", "inject"],
        "timeout_ms": 5000
      }
    ],
    "UserPromptSubmit": [
      {
        "type": "command",
        "command": ["node", "{{HOOK_BUNDLE}}", "codex", "capture-prompt"],
        "timeout_ms": 2000
      }
    ],
    "PostToolUse": [
      {
        "type": "command",
        "command": ["node", "{{HOOK_BUNDLE}}", "codex", "capture-tool"],
        "timeout_ms": 2000
      }
    ],
    "SessionEnd": [
      {
        "type": "command",
        "command": ["node", "{{HOOK_BUNDLE}}", "codex", "end"],
        "timeout_ms": 5000
      }
    ]
  }
}
